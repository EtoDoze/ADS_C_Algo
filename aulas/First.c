#include <stdio.h>

//GCC -> texto -> maquina

int main() {

    //Int main pq ao finalizar ela vai retornar 1 ou -1 ou qualquer valor inteiro

    //depurador é um programa que ajuda a encontrar erros e bugs

    //constante inteira é decimal

    // Ponto flutuante é uma float
    //char != string

    // %d chamada de valor após a virgula

    //long numeros grandes unsigned muito baixos, quando muito longos ou baixos, usa-se %ld invés de %d
    //numeros float, escreve-se %f, se eu quiser formatar numeros, %2f
    //em notação cientifica %e
    // caractere %c
    // %s string
    // %d inteiro
    // %f float
    // %lf double
    // % operador é o resto da divisão(módulo) de inteiros
    // ++ incremento e -- decremento, a + a + 1 == a ++ 1

    //scanf leitura do teclado
    // numeros não são nativos, string são, por isso que precisa especificar com & numeros

    //& -> adress of, pra resgatar endereços de memoria
    printf("\n Hello World!\n");
}